name: 🚀 API 명세 (API Specification)
description: 개발할 API의 상세 명세를 정의합니다.
title: "[파트] API 기능 요약"
labels: ["api"]
assignees: []

body:
  # --- 기본 정보 (6개 필드) ---
  - type: markdown
    attributes:
      value: "#기본 정보"

  - type: dropdown
    id: status # Status 열에 연결
    attributes:
      label: "Status"
      description: "API 개발의 현재 상태를 선택해주세요."
      options:
        - Todo
        - In Progress
        - Done
    validations:
      required: true

  - type: input
    id: function # function 열에 연결
    attributes:
      label: "function"
      description: "이 API로 구현하려는 핵심 기능을 작성해주세요."
      value: "ex. 로그인 처리"
    validations:
      required: true

  - type: dropdown
    id: http-method # HTTP Method 열에 연결
    attributes:
      label: "http-method"
      description: "사용할 HTTP Method를 선택해주세요."
      options:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
    validations:
      required: true

  - type: input
    id: api-path # API Path 열에 연결
    attributes:
      label: "api-path"
      description: "백엔드 RestController 메서드의 경로를 작성해주세요."
      value: "ex. /api/auth/login, /users/{userId}"
    validations:
      required: true

  - type: input
    id: return-type # Return Type 열에 연결
    attributes:
      label: "return-type"
      description: "메서드의 리턴 타입(DTO 또는 기본 타입)을 작성해주세요."
      value: "ex. ResponseEntity<LoginResponse>, String"
    validations:
      required: true

  # --- 요청 파라미터 통합 (1개 필드) ---
  - type: markdown
    attributes:
      value: "## ⚙️ Request Parameter 상세"

  - type: textarea
    id: request-parameter # Request Parameter 열에 연결
    attributes:
      label: "request-parameter"
      description: "Path Variable, Header, Query Parameter, Request Body DTO를 모두 이 한 곳에 작성합니다.(필요한것만 작성)"
      value: |
        ### Path Variable
        - userId (Long) : 조회할 사용자 고유 ID

        ### Request Header
        - X-USER-ID (Long, 필수) : 현재 요청을 보낸 사용자 ID

        ### Request Body (LoginRequest)
        ```java
        public class LoginRequest {
            private String email;
            private String password;
        }
        ```
    validations:
      required: true

  # --- 기타 참고 사항 섹션 ---
  - type: markdown
    attributes:
      value: "## 기타 참고 사항"

  - type: textarea
    id: notes
    attributes:
      label: "기타 참고 사항 (Response Body 요약 등)"
      value: "ex. 성공 시 UserInfo 객체 반환. 실패 시 ErrorResponse 객체 반환."